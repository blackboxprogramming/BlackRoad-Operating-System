# 014: Map Operations
# Modifying maps

person = {
  name: "Jordan",
  age: 25
}

# Add new property
person.email = "jordan@example.com"
show person

# Update existing property
person.age = 26
show "New age: {person.age}"

# Remove property
person.remove("email")
show person

# Get all keys
keys = person.keys()
show "Keys: {keys}"

# Get all values
values = person.values()
show "Values: {values}"
